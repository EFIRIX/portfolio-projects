# Portfolio Note

This snapshot excludes `.env`, virtual environments, logs, executable builds, and local runtime data.

Use `.env.example` for public configuration examples if this project is expanded later.

